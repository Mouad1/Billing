import { DataValueByKeyPipe } from './data-value-by-key.pipe';

describe('DataValueByKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new DataValueByKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
